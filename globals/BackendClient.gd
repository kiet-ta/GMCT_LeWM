extends Node

# Placeholder for FastAPI backend communication

func send_event(event_dict: Dictionary) -> void:
	# In the future, this will serialize the event_dict to JSON and use HTTPRequest 
	# to send it to the LeWorldModel training backend.
	# For now, we just simulate a successful send.
	pass
